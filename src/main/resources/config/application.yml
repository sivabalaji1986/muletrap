server:
  port: 8688
  servlet:
    contextPath: /api

#spring:
#  profiles:
#    default: database

management:
  endpoints:
    web:
      base-path: /actuator
      exposure:
        include: health  # Only expose /actuator/health
  endpoint:
    health:
      show-details: never
